# Consolidated Code Review - 2026-04-13

## Scope
- Agent 1: Diff Review (uncommitted + recent 5 commits)
- Agent 2: Holistic Review (project structure and agent readiness)
- Deduplication: completed across both agent outputs; no exact duplicate findings remained.

## Findings (by severity)

### High

1. **File:** `scripts/calculator.py:57`  
   **Severity:** high  
   **Category:** Diff  
   **Description:** Internal date sorting was removed from `_detect_status_change`, `_find_status_change_date`, `_detect_extreme_trend` and now relies on caller ordering. No guaranteed sort at the calculation entry was identified, so unordered input can silently produce wrong turning-point dates and trend signals.  
   **Suggested fix:** Enforce canonical ordering at `calculate_all_metrics()` entry with `df = df.sort_values("date").reset_index(drop=True)` or restore defensive sorting inside the three methods.

2. **File:** `scripts/data_fetcher.py:304`  
   **Severity:** high  
   **Category:** Diff  
   **Description:** `_fetch_sina_index()` now returns `None` immediately when `_convert_to_sina_symbol()` fails, removing prior `sh/sz` fallback behavior. This can break previously working index fetches for unmapped but valid A-share codes.  
   **Suggested fix:** Restore conservative `sh/sz` fallback for A-share symbols, or add strict config-time symbol mapping validation to fail fast before runtime.

3. **File:** `scripts/data_fetcher.py`  
   **Severity:** high  
   **Category:** Holistic  
   **Description:** `DataFetcher` applies a process-wide monkey patch to `requests.sessions.Session.request`. This creates global side effects across current and future HTTP call sites, reducing isolation and debuggability.  
   **Suggested fix:** Move UA injection to a dedicated `requests.Session` owned by `DataFetcher` (or explicit HTTP client wrapper) and avoid global monkey-patching.

### Medium

4. **File:** `.claude/settings.local.json:1`  
   **Severity:** medium  
   **Category:** Diff  
   **Description:** A local agent permission file was committed with elevated command allowances (`brew install`, `source`, etc.). Versioning this broadens command execution surface for collaborators.  
   **Suggested fix:** Keep this as machine-local config only and add it to `.gitignore` (or provide a minimal sanitized template without elevated defaults).

5. **File:** `scripts/test_csindex.py:1` (also removed: `scripts/test_pv_index.py`, `scripts/test_sina_indices.py`)  
   **Severity:** medium  
   **Category:** Diff  
   **Description:** Manual datasource smoke scripts were deleted without visible replacement. Given this repo has no pytest/unittest suite, regression coverage for datasource availability/fallback paths is reduced.  
   **Suggested fix:** Add a replacement smoke-check entrypoint (local or CI) covering `cs_index -> sina_index` fallback and US alternate source behavior.

6. **Directory:** `.` (repo root)  
   **Severity:** medium  
   **Category:** Holistic  
   **Description:** Root governance is centered on `CLAUDE.md` but lacks a first-class `AGENTS.md` hierarchy for multi-agent operational boundaries. This can cause inconsistent execution rules across tools/agents.  
   **Suggested fix:** Add root `AGENTS.md` as canonical policy entry, then optional scoped `AGENTS.md` under `scripts/` and `docs/`; keep `CLAUDE.md` aligned.

7. **File:** `implement.md`  
   **Severity:** medium  
   **Category:** Holistic  
   **Description:** Documentation appears drifted from current code behavior (commands/interfaces/config semantics), creating onboarding and automation risk.  
   **Suggested fix:** Mark as historical and move current source-of-truth to `README.md`/`CLAUDE.md`, or rewrite `implement.md` to match runtime reality with an explicit owner.

8. **File:** `.claude/review-loop-codex-prompt.txt`  
   **Severity:** medium  
   **Category:** Holistic  
   **Description:** Prompt hardcodes a timestamped output filename, reducing harness reusability and increasing risk of writing to wrong/overwritten target.  
   **Suggested fix:** Parameterize review output path via script/env placeholders and keep prompt generic.

9. **File:** `.github/workflows/update.yml`  
   **Severity:** medium  
   **Category:** Holistic  
   **Description:** Deployment workflow lacks an explicit pre-deploy quality gate (lint/compile/smoke), so logic regressions can be published with limited early detection.  
   **Suggested fix:** Add lightweight checks before deploy (e.g., `python -m py_compile`, dry-run smoke, template render sanity checks).

### Low

10. **File:** `scripts/main.py`, `scripts/backfill_archive.py`  
    **Severity:** low  
    **Category:** Holistic  
    **Description:** Runtime import path manipulation via `sys.path.insert(0, PROJECT_ROOT)` couples execution style to path injection and can introduce environment-specific behavior.  
    **Suggested fix:** Standardize package execution (`python -m scripts.main`) and package layout to remove path hacks.

11. **File:** `scripts/config.yaml`  
    **Severity:** low  
    **Category:** Holistic  
    **Description:** `schedule.update_time` appears unused in runtime logic, creating dead-config drift risk.  
    **Suggested fix:** Remove the field or wire it into explicit startup/runtime validation and behavior.

## Summary
- **Total issues:** 11
- **By severity:** high 3, medium 6, low 2, critical 0
- **Agents run:** Agent 1 (Diff Review), Agent 2 (Holistic Review)
- **Overall assessment:** The core architecture is workable, but current risk is concentrated in two areas: behavior-regression exposure in recent diffs (ordering/fallback changes) and maintainability drift (global side effects, documentation/harness inconsistencies).
