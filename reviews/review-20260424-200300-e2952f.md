# Consolidated Review - 2026-04-24 20:03:00 - e2952f

## Review Scope
- Agent 1: Diff Review（仅审查 `git diff` / `git diff --cached` / `git log --oneline -5` / `git diff HEAD~5` 涉及变更）
- Agent 2: Holistic Review（全局结构、文档、agent 协作准备度）
- Consolidation: 主线程已去重并做关键点复核

## Findings

### High

1. [H-01] 缺少 AGENTS.md 分层规范，agent 行为不可复现
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io (root, scripts/, templates/, docs/, .github/workflows/)`
Line: `N/A`
Severity: `high`
Category: `Holistic`
Description: 仓库当前可见约束文件为 `CLAUDE.md`，缺少可自动发现的 `AGENTS.md`（尤其是目录级）。在没有当前会话注入上下文时，不同 agent 无法获得一致执行边界，协作结果可重复性差。
Suggested fix: 新增 root `AGENTS.md`（模块地图、命令、验证策略、禁区），并在 `scripts/`、`templates/`、`.github/workflows/`、`docs/` 增加目录级 AGENTS；必要时提供 `CLAUDE.md` 兼容入口（可软链）。

2. [H-02] 核心实现文档与现状漂移，容易误导维护与后续 agent
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/implement.md`
Line: `23, 78-83, 112, 238-242`
Severity: `high`
Category: `Holistic`
Description: `implement.md` 仍声明“完全无本地存储”、旧接口与旧 source 类型（如 `index_zh_a_hist`、`eastmoney_sector`、`final_term`），与当前代码（如 `scripts/ranking_history.json` 持久化、`cs_index/sina_index`）不一致，会把维护者和 agent 引向错误实现路径。
Suggested fix: 将 `implement.md` 标为归档并停止作为事实源，或按当前代码重写；文档头加入 `last_verified_commit` 与失效规则。

3. [H-03] CI 无测试闸门，部署前缺少自动化回归保护
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/CLAUDE.md; /Users/loopq/dev/git/loopq/trend.github.io/.github/workflows/update.yml`
Line: `CLAUDE.md:48; update.yml:39-62`
Severity: `high`
Category: `Holistic`
Description: 项目明确无 pytest/unittest，workflow 仅安装依赖并直接执行 `scripts/main.py`，没有单测/静态检查/契约测试。agent 驱动变更后的回归风险无法在 CI 前置拦截。
Suggested fix: 补最小自动化测试集（至少覆盖 `calculator`、`generator`、`apply_rank_changes`、`should_deploy` 分支），并在部署步骤前强制 `test + lint` 通过。

### Medium

4. [M-01] 新浪代码兜底逻辑收窄，未映射代码可能直接取数失败
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/data_fetcher.py`
Line: `313-315`（关联 `_convert_to_sina_symbol` at `211-231`）
Severity: `medium`
Category: `Diff`
Description: 近期变更把 `_fetch_sina_index` 的兜底改为“无法转换就直接 `return None`”。虽然 `_convert_to_sina_symbol` 有部分推断规则，但对未覆盖首字符的代码会直接失败，可能抬高失败率并触发主流程中止。
Suggested fix: 恢复兼容兜底策略（至少保留原有 `3 -> sz`、其他按既有规则兜底），并增加“未映射代码”的回归测试。

5. [M-02] 删除手工验证脚本后未补等价自动化回归
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/test_csindex.py; /Users/loopq/dev/git/loopq/trend.github.io/scripts/test_pv_index.py; /Users/loopq/dev/git/loopq/trend.github.io/scripts/test_sina_indices.py`
Line: `N/A`
Severity: `medium`
Category: `Diff`
Description: 近期变更涉及 `main/data_fetcher/calculator/generator` 关键路径，同时删除了 3 个手工验证脚本，且未新增自动化覆盖关键门控与排名逻辑，回归只能运行时暴露。
Suggested fix: 新增最小 fixture+mock 测试，覆盖 `should_deploy` true/false、Sina fallback、`apply_rank_changes`（error/非error）行为。

6. [M-03] 仓库提交本地 agent 高权限配置，扩大供应链风险面
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/.claude/settings.local.json`
Line: `1-9`
Severity: `medium`
Category: `Diff`
Description: 文件允许 `Bash(brew install:*)` 与 `Bash(source:*)`，若被团队或自动化 agent 直接采纳，会显著扩大命令执行面。
Suggested fix: 该文件改为本地忽略（不入库），或将权限收紧为只读白名单并对安装命令强制人工确认。

7. [M-04] 全局 monkey-patch `requests.Session.request` 产生隐式副作用
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/data_fetcher.py`
Line: `95-124`
Severity: `medium`
Category: `Holistic`
Description: `_ensure_requests_patched()` 直接改写全局 `Session.request`，影响进程内所有 requests 调用，增加调试难度并提升跨模块耦合。
Suggested fix: 改为 DataFetcher 自管 session/header 注入，把 UA 策略限制在抓取模块边界内。

8. [M-05] 配置缺少 schema 校验，配置与代码耦合隐藏在运行时
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/__init__.py; /Users/loopq/dev/git/loopq/trend.github.io/scripts/main.py; /Users/loopq/dev/git/loopq/trend.github.io/scripts/data_fetcher.py`
Line: `__init__.py:6-9; main.py:84-88, 218; data_fetcher.py:38-57`
Severity: `medium`
Category: `Holistic`
Description: 配置加载仅 `yaml.safe_load`，无启动期结构校验；主流程直接索引 `code/name/source`；映射又散落在代码常量中。配置改动的错误会延迟到运行中段暴露。
Suggested fix: 引入显式配置模型（Pydantic/dataclass）做 fail-fast 校验，并将符号/交易所映射统一建模。

9. [M-06] 入口脚本依赖 `sys.path` 注入，执行上下文敏感
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/main.py; /Users/loopq/dev/git/loopq/trend.github.io/scripts/backfill_archive.py`
Line: `main.py:20-21; backfill_archive.py:19-20`
Severity: `medium`
Category: `Holistic`
Description: 两个入口通过 `sys.path.insert` 动态注入项目根目录，导致运行方式与 cwd 绑定，降低可移植性与可测试性。
Suggested fix: 统一采用 `python -m scripts.main` / `python -m scripts.backfill_archive`，并在 `pyproject.toml` 定义 console scripts，移除 path hack。

10. [M-07] 编排逻辑集中且重复，策略变更需多点同步
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/main.py; /Users/loopq/dev/git/loopq/trend.github.io/scripts/backfill_archive.py`
Line: `main.py:130-318; backfill_archive.py:60-110, 112-212`
Severity: `medium`
Category: `Holistic`
Description: `main()` 负担参数、门控、抓取、计算、失败率、持久化、渲染多个职责；`backfill_archive.py` 复制相似处理。后续改动容易漏改，agent 协作成本高。
Suggested fix: 抽离统一 pipeline/use-case 层（如 `UpdateService`、`IndexProcessor`），CLI 仅做参数解析与编排调用。

11. [M-08] 依赖未锁定，环境复现稳定性不足
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/requirements.txt`
Line: `1-6`
Severity: `medium`
Category: `Holistic`
Description: 依赖仅使用 `>=` 下限，缺少 lock/constraints。不同时间或机器安装版本可能漂移，导致“同代码不同结果”。
Suggested fix: 引入锁定文件（`requirements-lock.txt` 或 pip-tools constraints），并在 CI 固定安装策略。

### Low

12. [L-01] `--enable-analytics` 本地测试模式可能注入真实 GA ID
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/main.py`
Line: `225-229`
Severity: `low`
Category: `Diff`
Description: 注释写“本地测试用假 ID”，但实现优先读取 `GA_MEASUREMENT_ID` 环境变量。本地若配置真实 ID，会导致测试页写入真实统计。
Suggested fix: `--enable-analytics` 分支强制使用假 ID，或引入显式参数 `--ga-measurement-id` 并默认禁用环境变量回落。

13. [L-02] 归档页帮助弹窗结构与新样式体系不一致
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/templates/archive_detail.html; /Users/loopq/dev/git/loopq/trend.github.io/docs/css/style.css`
Line: `archive_detail.html:219; style.css:212, 537`
Severity: `low`
Category: `Diff`
Description: 归档页仍用 `.help-table`，而首页改为 `.help-list`。同时全局表格 `min-width: 900px` 与弹窗 `overflow-x: hidden` 组合可能导致小屏说明裁切。
Suggested fix: 统一归档页到 `.help-list` 结构，或给 `.help-table` 增加响应式规则并允许弹窗内横向滚动。

14. [L-03] 生成产物与源码同仓高耦合，评审噪音偏高
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/docs/archive; /Users/loopq/dev/git/loopq/trend.github.io/docs/index.html; /Users/loopq/dev/git/loopq/trend.github.io/scripts/ranking_history.json`
Line: `N/A`
Severity: `low`
Category: `Holistic`
Description: 仓库同时承载大量生成物与源码，日常检索和评审容易混入非源代码噪音，agent 误改风险上升。
Suggested fix: 明确 source-of-truth 与产物边界（例如仅 CI 更新生成物、PR 默认忽略生成物差异）。

15. [L-04] 调度配置与页面文案存在漂移
File/Dir: `/Users/loopq/dev/git/loopq/trend.github.io/scripts/config.yaml; /Users/loopq/dev/git/loopq/trend.github.io/templates/index.html`
Line: `config.yaml:2-3; index.html:29`
Severity: `low`
Category: `Holistic`
Description: 配置定义 `schedule.update_time: 16:00`，但首页文案写死“早间 08:00 更新前一天行情”，且主流程未消费该配置。
Suggested fix: 页面文案改为配置驱动，或删除未使用配置并统一单一事实源。

## Summary
- Total issues: `15`
- Severity breakdown: `critical 0 | high 3 | medium 8 | low 4`
- Agents ran: `2`
  - `Bohr` (Diff Review)
  - `Anscombe` (Holistic Review)
- Overall assessment: 近期改动未见立即致命缺陷，但存在一组会持续放大维护成本和回归概率的结构性问题（测试闸门缺失、文档漂移、配置/入口与全局副作用）。建议先处理 High 项，再按 Medium 中的“测试与边界收敛”优先级推进。
