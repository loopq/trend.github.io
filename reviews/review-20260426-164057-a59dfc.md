# Consolidated Code Review - 2026-04-26 16:40:57 (a59dfc)

## 审查范围与执行方式
- Agent 1（Diff）：仅审查 `git diff`、`git diff --cached`、`git log --oneline -5`、`git diff HEAD~5` 覆盖的改动。
- Agent 2（Holistic）：审查项目整体结构、文档与 agent 可维护性。
- 去重结果：未发现完全重复项，以下为去重后清单。

## Findings（按严重级别排序）

### High

1. **[Diff]**
   - File: `.github/workflows/quant.yml:169`
   - Severity: `high`
   - Category: `Diff`
   - Description: `morning-reconcile` 在 `--writer-mode commit` 下会触发 `git commit`，但 git 身份配置出现在后续步骤（177-178 行）。当 runner 无全局 git 配置时会直接失败。
   - Suggested fix: 在执行 `python -m scripts.quant.run_signal morning-reconcile --writer-mode commit` 前统一执行 `git config user.name/user.email`（建议在 job 开始阶段一次性配置）。

2. **[Diff]**
   - File: `scripts/quant/run_signal.py:171`
   - Severity: `high`
   - Category: `Diff`
   - Description: “上一工作日”仅按周末判断（`weekday >= 5`），未处理法定休市日。周中假期会指向非交易日，导致找不到昨日信号并跳过 close-confirm，遗留 provisional 状态。
   - Suggested fix: 引入交易日历回溯真实上一交易日（与 `is_trading_day` 语义统一），若无信号文件继续回溯到最近有信号的交易日。

3. **[Holistic]**
   - File/Dir: `. (repo root), scripts/, docs/, .github/workflows/`
   - Severity: `high`
   - Category: `Holistic`
   - Description: 关键目录缺少可执行的 `AGENTS.md` 规范，agent 协作约束不完整，跨目录改动容易失控。
   - Suggested fix: 在 root、`scripts/`、`scripts/quant/`、`docs/quant/`、`.github/workflows/` 增加 `AGENTS.md`，明确目录职责、可改范围、测试门槛与常见坑。

4. **[Holistic]**
   - File/Dir: `. (repo root), scripts/, docs/, .github/workflows/`
   - Severity: `high`
   - Category: `Holistic`
   - Description: 缺少“每个 `AGENTS.md` 对应 `CLAUDE.md` symlink”的兼容层，Claude/Codex 规则入口不一致。
   - Suggested fix: 为每个新增 `AGENTS.md` 创建同目录 `CLAUDE.md` 软链，保持单一规则源。

5. **[Holistic]**
   - File/Dir: `.github/workflows/quant-test.yml`, `scripts/main.py`, `scripts/data_fetcher.py`, `scripts/calculator.py`, `scripts/generator.py`
   - Severity: `high`
   - Category: `Holistic`
   - Description: CI 自动化测试主要覆盖 quant 子系统，主链路（每日更新/渲染/归档）缺少 PR 级回归闸门。
   - Suggested fix: 增加主链路 pytest（mock 数据源、模板渲染、`should_deploy` 判定），并在 workflow 对 `scripts/*.py`、`templates/*` 变更强制执行。

6. **[Holistic]**
   - File/Dir: `docs/quant/lib/writer.js`, `docs/quant/settings.html`
   - Severity: `high`
   - Category: `Holistic`
   - Description: 浏览器端直接持有 PAT 并执行 Git 写入，客户端与服务端安全边界混合，令牌泄露即导致仓库写权限泄露。
   - Suggested fix: 将写操作迁移到服务端受控通道（GitHub App、短时令牌或 workflow dispatch 中转）；前端仅提交意图与展示结果。

### Medium

7. **[Diff]**
   - File: `.github/workflows/quant.yml:111`
   - Severity: `medium`
   - Category: `Diff`
   - Description: signal 前置检查仅跳过周末，不处理法定休市日，节后可能误报 `morning-reconcile` 未执行，降低告警可信度。
   - Suggested fix: 将 `PREV_WORKDAY` 计算改为统一“上一交易日”函数（复用 Python 模块，避免 workflow 内联脚本漂移）。

8. **[Diff]**
   - File: `scripts/git-hooks/pre-commit:34`
   - Severity: `medium`
   - Category: `Diff`
   - Description: 回测同步校验只匹配 `docs/agents/backtest/v9-*.md`，版本号硬编码；后续 `v10-*` 或其他命名会绕过校验。
   - Suggested fix: 放宽为 `docs/agents/backtest/*.md` 或维护集中白名单配置，避免版本耦合。

9. **[Diff]**
   - File: `scripts/quant/tests/check_per_module_coverage.py:35`
   - Severity: `medium`
   - Category: `Diff`
   - Description: 本次关键改动涉及 `run_signal.py`（`morning-reconcile`/`close-confirm`），但覆盖率门禁排除了该文件，且缺少相应 CLI 回归测试。
   - Suggested fix: 增加 CLI 回归测试（含 `--realtime auto` 与节假日回溯场景），并将关键分支纳入门禁或设置独立最小覆盖阈值。

10. **[Holistic]**
   - File/Dir: `scripts/quant/run_signal.py:52-58`, `.github/workflows/quant.yml`
   - Severity: `medium`
   - Category: `Holistic`
   - Description: `notifier-mode=feishu` 且缺少 `FEISHU_WEBHOOK_URL` 时静默降级为 dry-run，形成“流程成功但通知未发”的隐性失败。
   - Suggested fix: 在 feishu 模式缺失 webhook 时 fail-fast（非零退出），并在 workflow 前置校验 secrets。

11. **[Holistic]**
   - File/Dir: `scripts/quant/config.yaml`, `.github/workflows/quant.yml`, `.github/workflows/quant-heartbeat.yml`
   - Severity: `medium`
   - Category: `Holistic`
   - Description: 触发时刻和运行语义在多个文件硬编码（14:48/14:50/14:55），存在配置漂移风险。
   - Suggested fix: 建立单一时间配置源，并由脚本校验/生成 workflow 时间点。

12. **[Holistic]**
   - File/Dir: `scripts/main.py:20-21`
   - Severity: `medium`
   - Category: `Holistic`
   - Description: 使用 `sys.path.insert` 注入导入路径，包边界依赖执行目录，运行方式脆弱。
   - Suggested fix: 采用标准包入口（如 `python -m scripts.main` 或 console script），移除运行时路径篡改。

13. **[Holistic]**
   - File/Dir: `scripts/backtest/run_v5.py`, `scripts/quant/build_quant_backtest.py`, `scripts/data_fetcher.py`
   - Severity: `medium`
   - Category: `Holistic`
   - Description: 多个 400-600 行脚本混合 CLI、业务、I/O，职责过重，后续改动回归风险高。
   - Suggested fix: 拆分为 `cli`（参数）、`service`（业务）、`adapter`（I/O）三层，入口文件保持薄层。

### Low

14. **[Diff]**
   - File: `docs/quant/style.css:379`
   - Severity: `low`
   - Category: `Diff`
   - Description: backtest 新增样式复用了全局类 `.empty-state`，可能覆盖 index/history 现有样式并造成跨页 UI 偏差。
   - Suggested fix: 使用命名空间（如 `.backtest-viewer .empty-state`）或专用类名，避免全局污染。

15. **[Holistic]**
   - File/Dir: `README.md`
   - Severity: `low`
   - Category: `Holistic`
   - Description: README 主要覆盖主链路，quant/backtest 的入口、secrets 矩阵与故障排查缺少统一导航。
   - Suggested fix: 增加“子系统导航 + 运维矩阵”章节，统一列出命令、workflow、secrets 与验证步骤。

## Summary
- Total issues: **15**
- Severity breakdown: `critical 0` / `high 6` / `medium 7` / `low 2`
- Agents ran: **2**（Diff Review, Holistic Review）
- Overall assessment: 最近改动存在几处高风险流程缺陷（交易日计算、workflow 提交身份时序），项目层面存在测试闸门不完整与前端 PAT 暴露风险，建议先处理 High 项后再推进新功能迭代。
