> Design: ./2026-05-10-donchian-200-design.html

# C Donchian 突破（月线 10/5）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.
>
> **Plan 是 self-contained**：每个 task 含完整代码 / 测试 / 验收命令，implementer 不必跳读 design.html（design 仅用于全局背景理解：架构 + 验收 + 风险）。

**Goal:** 在 cycle 1 已就绪的 equal-weight aggregator 框架下，落地 Donchian 通道突破策略（月线 entry_window=10 / exit_window=5）+ 注册 `donchian-200` + 跑 4 策略对比报告（baseline / bear / faber / donchian）on combined-27。

**Architecture:** 复用 cycle 1 全部基础设施。仅新增：(1) `DonchianBreakoutDecider` 类（自维护 close buffer，required_indicators=()）；(2) `@register("donchian-200")` 工厂；(3) 11 个单测；(4) 1 份 4 策略 compare 报告。零新增 aggregator / portfolio_window 函数。

**Tech Stack:** Python 3.9+、pandas、pytest。

**隔离铁律：** 所有改动对 v9-baseline / v9.3-bear / faber-gtaa 零行为变更。Task 5 是硬门槛——任何破坏现有数值的改动必须回退。Base SHA `1bb961c`（与 cycle 1 同基线，cycle 1 commit fd60c6c 之后）。

---

## Pre-flight

- [ ] **P1: 确认当前分支干净 + 现有测试 baseline**

  ```bash
  cd /Users/loopq/dev/git/loopq/trend.github.io
  git status   # 应为 nothing to commit
  source venv/bin/activate
  pytest scripts/backtest/test_*.py --tb=short 2>&1 | tail -3
  ```

  Expected: `62 passed`（cycle 1 后 baseline；cycle 2 加 11 个测试后期望达 73）

---

## Task 1: 写 11 个失败测试（TDD 红）

**Files:**
- Modify: `scripts/backtest/test_strategy_builtin.py`（在文件末尾追加 1 个测试 class + 1 个注册测试）

- [ ] **Step 1: 在 test_strategy_builtin.py 末尾追加测试**

```python
# ---------- DonchianBreakoutDecider ----------

from scripts.backtest.strategy.builtin import DonchianBreakoutDecider


def _bar(close, name=None):
    """构造 K 线（Donchian 仅用 close）。"""
    s = pd.Series({
        "open": close, "high": close + 1, "low": close - 1, "close": close,
    })
    if name is not None:
        s.name = name
    return s


def _feed(decider, cycle, closes, position_shares=0):
    """喂一段 close 序列给 decider；返回最后一根的信号。"""
    sig = None
    for c in closes:
        sig = decider.decide(cycle=cycle, bar=_bar(c), position_shares=position_shares)
    return sig


class TestDonchianBreakoutDecider:
    def setup_method(self):
        self.d = DonchianBreakoutDecider(entry_window=10, exit_window=5)

    def test_buffer_warm_up_no_signal(self):
        """buffer 长度 < entry_window 时不产生 BUY，即使 close 巨幅上涨。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        # 喂 9 根（差 1 根填满），最后一根 close 远高于前 9 根
        sig = _feed(d, "M", [100] * 9 + [200], position_shares=0)
        assert sig is None

    def test_breakout_above_max_buys(self):
        """buffer 满 (entry_window=10) + 第 11 根 close > 历史最高 + 空仓 → BUY。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        # 前 10 根 close = 100..109，第 11 根 = 110（严格 > max=109）
        closes = list(range(100, 110)) + [110]
        sig = _feed(d, "M", closes, position_shares=0)
        assert sig is not None
        assert sig.action == "BUY"
        assert sig.cycle == "M"
        assert sig.price == pytest.approx(110)

    def test_no_breakout_no_signal(self):
        """buffer 满 + close ≤ 历史最高 + 空仓 → None。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        closes = list(range(100, 110)) + [108]  # 108 ≤ max(100..109)=109
        sig = _feed(d, "M", closes, position_shares=0)
        assert sig is None

    def test_close_equals_max_no_breakout(self):
        """close == max 严格不算突破（必须严格 >）。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        closes = list(range(100, 110)) + [109]  # 109 == max(100..109)
        sig = _feed(d, "M", closes, position_shares=0)
        assert sig is None

    def test_breakdown_below_min_sells(self):
        """持仓 + close < 历史最低 → SELL。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        # 前 5 根 close = 105..109（warm up exit_window=5），第 6 根 = 100（< min=105）
        # 但 buffer 不够 entry_window=10，先填充 5 根（不触发 BUY，因 < entry_window）
        # 然后再喂 1 根 close=100，position_shares=1.0 → 应 SELL（buffer ≥ exit_window=5）
        sig = _feed(d, "M", [105, 106, 107, 108, 109], position_shares=1.0)
        assert sig is None  # buffer 满 5 根但还没第 6 根来比较
        sig = d.decide(cycle="M", bar=_bar(100), position_shares=1.0)  # close=100 < min(105..109)=105
        assert sig is not None
        assert sig.action == "SELL"
        assert sig.price == pytest.approx(100)

    def test_no_breakdown_no_signal(self):
        """持仓 + close ≥ 历史最低 → None。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        _feed(d, "M", [105, 106, 107, 108, 109], position_shares=1.0)
        sig = d.decide(cycle="M", bar=_bar(106), position_shares=1.0)  # 106 >= min=105
        assert sig is None

    def test_close_equals_min_no_breakdown(self):
        """close == min 严格不算跌破（必须严格 <）。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        _feed(d, "M", [105, 106, 107, 108, 109], position_shares=1.0)
        sig = d.decide(cycle="M", bar=_bar(105), position_shares=1.0)  # 105 == min=105
        assert sig is None

    def test_already_in_no_resignal(self):
        """已持仓 + close 又创新高 → None（不重复 BUY）。"""
        d = DonchianBreakoutDecider(entry_window=10, exit_window=5)
        # 喂前 10 根填满 buffer
        _feed(d, "M", list(range(100, 110)), position_shares=0)
        # 第 11 根 close=200 + position_shares=1.0 → 应 None（因为已持仓）
        sig = d.decide(cycle="M", bar=_bar(200), position_shares=1.0)
        assert sig is None

    def test_nan_close_returns_none(self):
        sig = self.d.decide(cycle="M", bar=_bar(float("nan")), position_shares=0)
        assert sig is None

    def test_required_indicators_empty(self):
        assert DonchianBreakoutDecider().required_indicators == ()

    def test_custom_window(self):
        """entry_window=8 / exit_window=4 自定义参数。"""
        d = DonchianBreakoutDecider(entry_window=8, exit_window=4)
        # 8 根 warm up（填满 entry_window），第 9 根 close > max → BUY
        closes = list(range(100, 108)) + [108]  # max(100..107)=107，108>107
        sig = _feed(d, "M", closes, position_shares=0)
        assert sig is not None and sig.action == "BUY"


def test_donchian_200_registered():
    _reload_builtin()
    from scripts.backtest.strategy import get
    s = get("donchian-200")
    assert s.name == "donchian-200"
    assert s.filters == ()
    assert s.cycles == ("M",)
    assert s.aggregator == "equal-weight"
    # 用 reload 后的 class 引用（避免 isinstance 与旧引用比对失败，参考 cycle 1 T7）
    from scripts.backtest.strategy.builtin import DonchianBreakoutDecider as _DonchianCls
    assert isinstance(s.decider, _DonchianCls)
    assert s.decider.entry_window == 10
    assert s.decider.exit_window == 5
```

- [ ] **Step 2: 跑测试确认失败（红）**

```bash
pytest scripts/backtest/test_strategy_builtin.py::TestDonchianBreakoutDecider scripts/backtest/test_strategy_builtin.py::test_donchian_200_registered -v 2>&1 | tail -15
```

Expected: `ImportError` on `DonchianBreakoutDecider`（类未实现）。

- [ ] **Step 3: 不 commit**（Task 2 一起 commit 测试 + 实现）

---

## Task 2: 实现 DonchianBreakoutDecider + 注册 donchian-200（TDD 绿）

**Files:**
- Modify: `scripts/backtest/strategy/builtin.py`（追加 `DonchianBreakoutDecider` 类 + `@register("donchian-200")` 工厂）

- [ ] **Step 1: 在 builtin.py 末尾追加 DonchianBreakoutDecider 类**

在 `FaberMonthlyMaDecider` 之后（在 `@register("v9-baseline")` 之前）追加：

```python
class DonchianBreakoutDecider:
    """Donchian 通道突破（月线版）。

    每根月线 K 线：
      若空仓 + buffer ≥ entry_window：close > max(buffer[-entry_window:]) → BUY
      若持仓 + buffer ≥ exit_window：close < min(buffer[-exit_window:]) → SELL
      close NaN → None
    决策完后追加当月 close 到 buffer，裁剪到最近 max(entry, exit) 个。

    与 FaberMonthlyMaDecider 区别：
    - Faber 比 close vs MA10（均值），Donchian 比 close vs max/min（极值）
    - Faber 的状态翻转高频（均值穿越易触发），Donchian 的状态翻转低频（必须创新高/破新低）
    - Donchian 自维护 close buffer，required_indicators=()，不依赖 _ensure_indicators

    默认 entry=10 / exit=5（月线 10/5 ≈ 日线 200/100，与海龟系统 1 同级别）。
    """

    name = "donchian-breakout-monthly"

    def __init__(self, entry_window: int = 10, exit_window: int = 5) -> None:
        self.entry_window = entry_window
        self.exit_window = exit_window
        self.required_indicators: Tuple[Tuple[str, str, int], ...] = ()
        self._close_buffer_by_cycle: Dict[str, List[float]] = {}

    def decide(self, *, cycle: str, bar: pd.Series, position_shares: float) -> Optional[Signal]:
        close = bar.get("close")
        if pd.isna(close):
            return None

        buf = self._close_buffer_by_cycle.setdefault(cycle, [])
        max_window = max(self.entry_window, self.exit_window)

        signal: Optional[Signal] = None
        if position_shares == 0 and len(buf) >= self.entry_window:
            entry_high = max(buf[-self.entry_window:])
            if close > entry_high:
                signal = Signal(
                    action="BUY", cycle=cycle, price=float(close),
                    bar_date=pd.Timestamp(bar.name) if bar.name is not None else pd.NaT,
                )
        elif position_shares > 0 and len(buf) >= self.exit_window:
            exit_low = min(buf[-self.exit_window:])
            if close < exit_low:
                signal = Signal(
                    action="SELL", cycle=cycle, price=float(close),
                    bar_date=pd.Timestamp(bar.name) if bar.name is not None else pd.NaT,
                )

        # 决策后追加当月 close（避免 look-ahead bias）
        buf.append(float(close))
        if len(buf) > max_window:
            del buf[: len(buf) - max_window]

        return signal
```

注意：与 Faber 同样用 `"BUY"` / `"SELL"` 字符串字面量，不引入 `from ... import BUY, SELL`。

- [ ] **Step 2: 注册 donchian-200 策略**

在 `builtin.py` 末尾（在 `_faber_gtaa` 之后）追加：

```python
@register("donchian-200")
def _donchian_200() -> Strategy:
    return Strategy(
        name="donchian-200",
        decider=DonchianBreakoutDecider(entry_window=10, exit_window=5),
        filters=(),
        cycles=("M",),
        aggregator="equal-weight",
    )
```

- [ ] **Step 3: 跑测试确认通过（绿）**

```bash
pytest scripts/backtest/test_strategy_builtin.py -v --tb=short 2>&1 | tail -20
```

Expected: 全过（cycle 1 后 31 测试 + 11 新 = 42 测试，全 passed）

- [ ] **Step 4: 跑全 backtest 套件零退化**

```bash
pytest scripts/backtest/test_*.py --tb=short 2>&1 | tail -3
```

Expected: 73 passed（62 + 11）

- [ ] **Step 5: Commit**

```bash
git add scripts/backtest/strategy/builtin.py scripts/backtest/test_strategy_builtin.py
git commit -m "[backtest] cycle2-T2: 实现 DonchianBreakoutDecider + 注册 donchian-200（月线 10/5 突破）"
```

---

## Task 3: 端到端联调（--list + --strategy 烟测）

**Files:** 无；纯验证

- [ ] **Step 1: --list 输出含 donchian-200**

```bash
source venv/bin/activate
python -m scripts.backtest.run --list 2>&1 | grep donchian
```

Expected: 输出含 `donchian-200` 一行。

- [ ] **Step 2: --strategy donchian-200 烟测**

```bash
python -m scripts.backtest.run --strategy donchian-200 --universe combined-27 --windows 3 2>&1 | tail -5
```

Expected:
- `加载 27 个指数数据 ...`
- `3 年 总 CAGR X.XX% / MDD -X.XX%`（数值合理：CAGR 通常 +0% ~ +20%、MDD 通常 -10% ~ -45%）
- 退出码 0

- [ ] **Step 3: 不 commit**（纯验证）

---

## Task 4: 隔离硬门槛（5 条隔离断言，**硬门槛**）

**Files:** 无；纯验收

复用 cycle 1 T9 的 5 条断言（cycle 2 base SHA 仍是 `1bb961c`）：

- [ ] **Step 1: 断言 1 ── scripts/quant/ + main.py + docs/ 零修改**

```bash
git diff 1bb961c..HEAD -- scripts/quant/ scripts/main.py docs/
```

Expected: 输出**完全为空**

- [ ] **Step 2: 断言 2 ── 全 pytest 套件 + test_signal_manual 通过**

```bash
pytest scripts/backtest/test_*.py --tb=short 2>&1 | tail -3
python -m scripts.backtest.test_signal_manual 2>&1 | tail -3
```

Expected: pytest 73 passed（62 + 11）+ test_signal_manual `All tests passed.`

- [ ] **Step 3: 断言 3 ── v9-baseline on v9 universe 数值不变**

```bash
python -m scripts.backtest.run --strategy v9-baseline --universe v9 --windows 3,5,8,10 2>&1 | tail -6
```

Expected: 3年 15.32%/-12.55%、5年 10.98%/-19.69%、8年 11.51%/-22.12%、10年 9.29%/-22.25% **逐字一致**

- [ ] **Step 4: 断言 4 ── v9-baseline + v9.3-bear on v9 universe 报告数值不变**

```bash
python -m scripts.backtest.run --compare v9-baseline,v9.3-bear --universe v9 --windows 3,5,8,10 2>&1 | tail -10
diff <(grep -E "^\| [0-9]+ 年" agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear.md) <(grep -E "^\| [0-9]+ 年" agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-v9universe.md) | head -40
```

Expected: 仅 Δ 行 label 结构差异（cycle 1 T8 的 plan 设计），数值列字符级一致

- [ ] **Step 5: 断言 5 ── v9-baseline + v9.3-bear on main-online universe 报告数值不变**

```bash
python -m scripts.backtest.run --compare v9-baseline,v9.3-bear --universe main-online --windows 3,5,8,10 2>&1 | tail -10
diff <(grep -E "^\| [0-9]+ 年" agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear.md) <(grep -E "^\| [0-9]+ 年" agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-mainonline.md) | head -40
```

Expected: 同上

- [ ] **Step 6: 5 条全过则**清理 step 4/5 跑出的临时裸文件名报告：

```bash
rm -f agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear.md
```

任意一条失败 → BLOCKED → 排查回滚。**本 task 不产生 commit**（纯验证）

---

## Task 5: 跑 4 策略 compare on combined-27

**Files:** 输出报告到 `agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-vs-faber-gtaa-vs-donchian-200.md`

- [ ] **Step 1: 跑 compare 命令**

```bash
source venv/bin/activate
time python -m scripts.backtest.run --compare v9-baseline,v9.3-bear,faber-gtaa,donchian-200 --universe combined-27 --windows 3,5,8,10 2>&1 | tee /tmp/4way-compare.log | tail -30
```

Expected: 退出码 0；总耗时 ≤ 8 分钟（27 指数 × 4 策略；缓存全有时纯 CPU）；输出含 4 段 `加载 27 个指数数据 ...` + 16 行 `N 年 总 CAGR ...% / MDD ...%`

- [ ] **Step 2: 检查报告文件生成**

```bash
ls -la agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-vs-faber-gtaa-vs-donchian-200.md
head -60 agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-vs-faber-gtaa-vs-donchian-200.md
```

Expected: 文件存在；表头有 7 行 per 窗口 = 28 行表数据：每窗口 4 行策略 + 3 行 Δ（bear/faber/donchian vs baseline）

- [ ] **Step 3: 人工 sanity check**

```bash
open /Users/loopq/dev/git/loopq/trend.github.io/agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-vs-faber-gtaa-vs-donchian-200.md
```

确认：
- 4 策略数值有差异（不全相同）
- donchian-200 数值合理（CAGR > 0 或接近、MDD < 0）
- 3 个 Δ 行符号正确

- [ ] **Step 4: 本 task 不 commit**（Task 6 加完中文解读后一起 commit）

---

## Task 6: 报告中文解读 + commit

**Files:** Edit `agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-vs-faber-gtaa-vs-donchian-200.md`

- [ ] **Step 1: 在报告顶部插入"一句话结论"段（参照 cycle 1 报告结构）**

模板（implementer 看 Task 5 实际数据填）：

```markdown
> Universe：combined-27（v9 14 主题/行业 + main-online 16 宽基/海外/商品 去重）
> 时间窗：3 / 5 / 8 / 10 年
> 数据终点：2026-04-24

---

## 一句话结论

**[根据实际数据填]**：donchian-200 vs baseline ΔCAGR = X.XXpp ~ X.XXpp（4 窗口）；vs faber-gtaa ΔCAGR = X.XXpp ~ X.XXpp。**Donchian 突破 vs Faber 均值** 同 timeframe 实测：[填胜负 + 主要差异点]。

具体看（ΔCAGR / ΔMaxDD vs baseline）：
- 3 年: bear ΔCAGR = X.XXpp / ΔMaxDD = X.XXpp; faber ΔCAGR = X.XXpp / ΔMaxDD = X.XXpp; donchian ΔCAGR = X.XXpp / ΔMaxDD = X.XXpp
- 5 年: ...
- 8 年: ...
- 10 年: ...

---
```

- [ ] **Step 2: 在"二、组合层对比"之后插入"突破策略 vs 均值策略"对照段**

```markdown
## 二、Donchian 突破 vs Faber 均值（同 timeframe 对照实验）

两策略都跑月线 + equal-weight + combined-27，唯一区别是 Decider 算法：
- **Faber**：close > MA10（10 月均值）→ BUY；close ≤ MA10 → SELL（均值穿越）
- **Donchian**：close > max(过去 10 月 close) → BUY；close < min(过去 5 月 close) → SELL（极值突破）

| 维度 | Faber 均值法 | Donchian 极值法 |
|---|---|---|
| 入场频率 | 较高（均值穿越易触发） | 较低（必须创 N 月新高） |
| 出场速度 | 较快（close ≤ MA 立即翻 DOWN） | 较慢（必须破 N/2 月新低） |
| 4 窗口 ΔCAGR vs baseline | 3年 [-X.XX] / 5年 [-X.XX] / 8年 [-X.XX] / 10年 [-X.XX] | 3年 [Y.YY] / 5年 [Y.YY] / 8年 [Y.YY] / 10年 [Y.YY] |

[填实际对照后给结论：哪个算法在 combined-27 上更胜？是入场严格性更重要还是出场敏捷性更重要？]

---
```

- [ ] **Step 3: 在分指数差异 3 个 Δ 子表之后插入"分指数模式"段**

```markdown
### 分指数模式

按 universe 子集看 donchian-200 vs baseline（参考上方第三个 Δ 子表）：
- **A 股宽基**（沪深300/上证50/中证500/中证1000/中证2000/创业板50/科创50/北证50）：[填观察]
- **A 股主题/行业**（光伏/白酒/医疗/5G/AI/智汽车/军工/新能/有色/化工）：[填观察]
- **港股**（HSI/HSCEI/HSTECH）：[填观察]
- **美股**（NDX/SPX）：[填观察]
- **加密 / 商品**（BTC/XAU/XAG）：[填观察]

总结：donchian-200 在 [大盘 / 主题 / 港股 / 商品] 上 [跑赢 / 跑输] baseline 较显著……

---
```

- [ ] **Step 4: 在报告末尾追加"四、后续方向"段**

```markdown
## 四、后续方向

### 当前 Donchian 实操判断

[填：Donchian 200 是否值得替换 baseline？或仅在某 universe 子集生效？vs Faber 哪个更接近"提收益"目标？]

### 调参方向（未来 Donchian 变体）

按代价从小到大：
1. **改 entry/exit 窗口**：(10, 5) → (12, 6) 更保守 / (8, 4) 更激进。注册新策略 `donchian-12-6` / `donchian-8-4` 即可，框架支持。
2. **位置规模管理**：海龟系统经典扩展是按 ATR 决定 position size。需要新 aggregator（不是 equal-weight 了）——留 cycle 5+。
3. **加 trailing stop**：用持仓最高价回撤 N% 强制清仓，比"破 5 月新低"更敏感。需要状态扩展。
4. **下一周期：A Dual Momentum（横截面 top-K）**——按之前规划进行；与 Faber/Donchian 的"全 universe in/out"互补。
```

- [ ] **Step 5: 把所有 `[填...]` 占位替换为基于 Task 5 实际数据的具体数字 + 观察**（implementer 看着数据填）

- [ ] **Step 6: Commit 报告**

```bash
git add -f agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-vs-faber-gtaa-vs-donchian-200.md
git commit -m "[backtest] cycle2-T6: 产出 v9-baseline vs v9.3-bear vs faber-gtaa vs donchian-200 四策略对比报告（含中文解读）"
```

---

## Task 7: 范围检查（生产隔离不变量）

**Files:** 无；纯检查

- [ ] **Step 1: 全部 commit 后再次 git diff 排除 scripts/quant/ + scripts/main.py + docs/**

```bash
git diff 1bb961c..HEAD --name-only | grep -E '^(scripts/quant/|scripts/main\.py|docs/)' || echo "OK: 生产 + 前端干净"
```

Expected: `OK: 生产 + 前端干净`

- [ ] **Step 2: 跑 quant 现有测试零退化**

```bash
pytest scripts/quant/tests/ --tb=short 2>&1 | tail -3
```

Expected: `179 passed`（与 cycle 1 / base SHA 一致）

- [ ] **Step 3: 跑 backtest 全套零退化**

```bash
pytest scripts/backtest/test_*.py --tb=short 2>&1 | tail -3
python -m scripts.backtest.test_signal_manual 2>&1 | tail -3
```

Expected: pytest 73 passed；test_signal_manual `All tests passed.`

- [ ] **Step 4: 本 task 不 commit。任何步骤失败 → 排查违反的不变量 → 回滚相关 commit**

---

## Plan 完成后

待 7 个 task 全过后给用户**周期 2 完成报告**：

- 列出 commit 序列（约 2-3 个 [backtest] commits：T2 实现 + T6 报告）
- 4 策略对比报告：`/Users/loopq/dev/git/loopq/trend.github.io/agents/results/2026-05-10-compare-v9-baseline-vs-v9.3-bear-vs-faber-gtaa-vs-donchian-200.md`
- 关键发现：Faber vs Donchian 同 timeframe 对照结论
- Reviewer Minor 建议汇总（用户决定回头补不补）
- 用户审完批准 → 进 cycle 3（Dual Momentum）

---

## Spec 覆盖度自查

| design.html § | 对应 Task |
|---|---|
| §0 隔离保证 | Task 4（5 条断言验收） |
| §1 背景与目标 | Plan header（Goal / Architecture） |
| §2 DonchianBreakoutDecider 规格 | Task 2（实现） |
| §3 donchian-200 策略注册 | Task 2 Step 2 |
| §4 equal-weight 流程复用 | Task 3（端到端烟测验证） |
| §5 测试范围 | Task 1（11 个测试） |
| §6 验收标准 | Task 4（隔离）+ Task 3（功能） |
| §7 报告内容 | Task 5（生成）+ Task 6（中文解读） |
| §8 关键不变量 | Task 4 + Task 7（范围检查） |
| §9 风险与权衡 | 无对应 task（已在 design 文档化） |
| §10 不在范围 | 无对应 task（说明性） |

11 章节全部映射，无遗漏。

## Self-review

- [x] **Spec coverage**：11 章节全映射
- [x] **Placeholder 扫描**：仅 Task 6 中文解读段有 `[填...]` 占位（implementer 看实际数据填，与 cycle 1 T11 同模式）；代码段无 TBD / TODO
- [x] **类型一致性**：`DonchianBreakoutDecider` / `entry_window` / `exit_window` / `donchian-200` 跨 task 命名一致
- [x] **完整代码**：每个 task 含 implementer 直接执行的完整 Python 代码 + Bash 验收命令；无需跳读 design.html 拿代码
- [x] **隔离铁律**：cycle 1 5 条断言全量复跑（Task 4）
