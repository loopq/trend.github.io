# Python
venv/
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# 日志
*.log

# 临时文件
*.tmp
scripts/backtest/.cache/
.coverage
coverage.json

# Quant 运行时本地产物（仅本地 demo / mock-test 需要的；线上由 workflow 写）
docs/data/quant/notify-outbox/*.json
docs/data/quant/cache/*.csv
# signals/{date}.json 不再 ignore：上线后由 quant.yml signal mode 在仓库生成 + commit + sync gh-pages
# 本地 demo 走 QUANT_DATA_ROOT=/tmp 隔离（mvp-plan §3.5.1）
